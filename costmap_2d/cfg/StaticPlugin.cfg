#!/usr/bin/env python

from dynamic_reconfigure.parameter_generator_catkin import ParameterGenerator, bool_t, double_t, int_t

gen = ParameterGenerator()

gen.add("enabled", bool_t, 0, "Whether to apply this plugin or not", True)

combo_enum = gen.enum([gen.const("Maximum", int_t,  0, "Take the maximum of the values"),
                       gen.const("Overwrite", int_t,  1, "Overwrite values"),
                       gen.const("Subtraction", int_t,  2, "LETHAL becomes FREE")],
                       "Method for combining layers enum")
gen.add("combination_method", int_t, 0, "Method for combining two layers", 1, edit_method=combo_enum)
exit(gen.generate("costmap_2d", "costmap_2d", "StaticPlugin"))
